# tiny-GPT
it is a tiny transformer which uses multi head attention layer to generate text.
It uses  decoder attention block to mask future words while processing current words.
It uses encode attention blocks to understand the whole sentence.
